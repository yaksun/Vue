import base from '../request/base'
import axios from '../request/http'

export default {
  // async getUserInfo ({commit, state}) {
  //   let res = await axios.post(base.url + '')
  //   commit('', res)
  // }
}
